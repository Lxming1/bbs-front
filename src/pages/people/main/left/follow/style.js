import styled from 'styled-components'

export default styled.div`
  .userList {
    min-height: calc(100vh - 400px);
    position: relative;
  }
  .pagination {
    padding: 20px 0;
    text-align: center;
  }
`
